/**
 * @description
 * The width at which the mobile view is displayed.
 * Units are in pixels.
 *
 * Subsitute with real value as there's build problem because of SSR
 * var(--mobile-width-scalar)
 */
export const mobileWidth = 998;
