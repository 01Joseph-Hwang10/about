import { css } from "@emotion/react";

const tagWrapper = css`
  display: flex;
  justify-content: flex-start;
  align-items: center;
  flex-wrap: wrap;
  word-break: keep-all;
`;

export default {
  tagWrapper,
};
